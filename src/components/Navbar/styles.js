import { styled } from "styled-components";

export const Nav = styled.nav`
    background-color: red;
    display: flex;
    padding: 1rem;
    font-weight: bold;

    span {
        font-size: 2rem;
    }
`